how to push the sourse code one repo to another repo ->
        git remote add destination https://github.com/JerinL/cloud-build-services.git
        git remote set-url origin https://github.com/JerinL/cloud-build-services.git
        git push --mirror destination
        git push --mirror origin


git repository set ->
        git init
Initialized empty Git repository in D:/KAFKA/.git/
         git remote add origin https://github.com/JerinL/kafka-demo.git






…or create a new repository on the command line
echo "# hibernate-query-practice" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/JerinL/hibernate-query-practice.git
git push -u origin main         





…or push an existing repository from the command line
git remote add origin https://github.com/JerinL/hibernate-query-practice.git
git branch -M main
git push -u origin main
